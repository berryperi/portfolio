# 포트폴리오 배포 방법

---

## 방법 비교

| 방법 | 난이도 | 비용 | 특징 |
|------|--------|------|------|
| GitHub + Vercel | 쉬움 | 무료 | 자동 배포, 링크 영구 고정 — **추천** |
| Netlify Drop | 매우 쉬움 | 무료 | 수동 업로드, 빠른 공유용 |

---

## 방법 1. GitHub + Vercel (추천)

### 추천 이유
- 코드 수정 후 `git push`만 하면 자동으로 재빌드 + 재배포
- URL이 영구적으로 고정되어 이력서/명함에 사용 가능
- 포트폴리오 수정 이력이 GitHub에 기록됨

### 1단계 — GitHub에 올리기

```bash
# D:\project\port 경로에서 실행
git init
git add .
git commit -m "init portfolio"
```

1. [github.com](https://github.com) 에서 새 repository 생성
2. 아래 명령어로 연결 후 push

```bash

git remote add origin https://github.com/본인계정/port.git
git push -u origin main
```

### 2단계 — Vercel 연동

1. [vercel.com](https://vercel.com) 접속 → GitHub 계정으로 로그인
2. **"Add New Project"** 클릭
3. 방금 올린 GitHub repo 선택
4. Framework: **Vite** 자동 감지 확인
5. **Deploy** 클릭

→ `https://포트폴리오이름.vercel.app` 링크 생성 완료

### 이후 업데이트 방법

```bash
# 코드 수정 후
git add .
git commit -m "수정 내용"
git push
```

push 하면 Vercel이 자동으로 감지해서 재배포. 링크는 그대로 유지됨.

---

## 방법 2. Netlify Drop (빠른 공유용)

### 주의사항
- 수정할 때마다 `npm run build` 후 dist 폴더를 수동으로 재업로드 필요
- 재배포 시 URL이 바뀔 수 있음

### 배포 순서

1. 빌드 실행

```bash
npm run build
```

2. [app.netlify.com/drop](https://app.netlify.com/drop) 접속
3. `D:\project\port\dist` 폴더를 브라우저에 드래그 앤 드롭
4. 링크 생성 완료

---

## 참고

- Vercel 무료 플랜으로 개인 포트폴리오 운영 충분
- 커스텀 도메인(예: `홍길동.dev`) 연결도 Vercel에서 무료로 가능